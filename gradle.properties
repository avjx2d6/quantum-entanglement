org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

mod_id=quantumitems
mod_name=Quantum Entanglement
mod_version=0.4.6
mod_group_id=com.quantumitems

minecraft_version=1.21.1
neo_version=21.1.235

# Ponder — in-game documentation library (Create not required). Embedded into
# our jar via jarJar, together with Flywheel, so players need no extra mod.
# https://maven.createmod.net  ·  net.createmod.ponder
# Coordinate composed in build.gradle as ponder-neoforge:<ver>+mc<mc_version>.
ponder_version=1.0.87
flywheel_version=1.0.4
